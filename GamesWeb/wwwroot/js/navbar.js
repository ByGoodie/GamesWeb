function navigationBtn(caller) {
    var navs = document.getElementsByClassName("nav-link");
    
    navs.forEach(element => {
        
    });

    navs.forEach(element => {
        element.classList.forEach()
    });
}